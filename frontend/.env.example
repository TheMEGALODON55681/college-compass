# Base URL the frontend calls for the backend API. Defaults to
# http://localhost:8000 if unset - only needed when the backend runs
# somewhere other than localhost, e.g. a real deploy.
VITE_API_BASE=http://localhost:8000
