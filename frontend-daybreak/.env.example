# Base URL the frontend calls for the backend API. Empty by default so dev
# requests go through the Vite proxy (vite.config.ts) to http://localhost:8000.
# Set this to the real backend origin for any deploy where the frontend isn't
# proxied, e.g. VITE_API_BASE=https://api.example.com
VITE_API_BASE=
